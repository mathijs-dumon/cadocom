
What has been done:
Server side:
    - REST API's:
        - gift list API
        - user api (registering, login, logout)
Client side:
    - basic UI
    - login, Gift adding

What still needs to be done:
Server side:
    - REST API's:
        - wishlist API
        - combining wish, gift and user models (allowing users to have personal lists)
        - user profile api
        - ...
Client side:
    - logout
    - profile
    - when it's there more REST api interfaces